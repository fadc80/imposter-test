def handle(global) {
  global.with {
    logger.info("Service handler: <!--SERVICE_LOWER-->")
    respond {
      withStatusCode(200) and()
      withFile("services/<!--SERVICE_LOWER-->/data/default.json")
    }
  }
}
